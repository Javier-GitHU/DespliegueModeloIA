Para poder hacer uso de al menos la api, ya que la parte del docker y la de Kubernetes no funcionan, se debe usar este comando dentro de la carpeta de ai-service python -m uvicorn main:app --reload --port 8000 
y luego meterte en este enlace http://localhost:8000/docs y ahi podra probarla
